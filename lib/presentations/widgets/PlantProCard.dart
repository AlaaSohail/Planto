import 'package:flutter/material.dart';

class PlantProCard extends StatelessWidget {
  const PlantProCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(

    );
  }
}
